//5. The user enters 1 positive number (N). Print the Nth number of the fibonacci series.
//In the fibonacci series, each next number is the sum of the previous two.
//The first and second are considered equal to 1.

/*function getNumberFromUser(message) {
  let userValue;
  let flag = false;
  do {
    if (flag) {
      alert("You have to enter number");
    }
    userValue = +prompt(message);
    flag = true;
  } while (isNaN(userValue));
  return userValue;
}
const number = getNumberFromUser(`Enter number a`);
let n1 = 1,
  n2 = 1,
  nextTerm;
console.log("Fibonacci Series:");
calculateFibonacci(n1, n2);
console.log(n2);

function calculateFibonacci(N1, N2) {
  for (let i = 3; i <= number; i++) {
    nextTerm = n1 + n2;
    n1 = n2;
    n2 = nextTerm;
  }
}
*/
