//1. The user enters 1 number (A). Print all numbers from 1 to 1000 that are divisible by A.
/*function getNumberFromUser(message) {
  let userValue;
  let flag = false;
  do {
    if (flag) {
      alert("You have to enter number");
    }
    userValue = +prompt(message);
    flag = true;
  } while (isNaN(userValue));
  return userValue;
}
const a = getNumberFromUser(`Enter number a`);
let i;
calculateLoop(i, a);

function calculateLoop(valI, valA) {
  for (let i = 1; i < 1000; i++) {
    if (i % a === 0) {
      console.log(i);
    }
  }
}
*/
