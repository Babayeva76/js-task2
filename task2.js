//2. The user enters 1 number (A).
//Find the number of positive integers whose square is less than A (n^2 < A).

/*function getNumberFromUser(message) {
  let userValue;
  let flag = false;
  do {
    if (flag) {
      alert("You have to enter number");
    }
    userValue = +prompt(message);
    flag = true;
  } while (isNaN(userValue));
  return userValue;
}
const a = getNumberFromUser(`Enter number a`);
let i;
calculateLoop(i, a);

function calculateLoop(valI, valA) {
  for (let i = 0; i < a; i++) {
    if (i ** 2 < a) {
      console.log(i);
    }
  }
}
*/
